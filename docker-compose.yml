services:
  db:
    image: postgis/postgis:15-3.3
    container_name: guaikediaz
    environment:
      POSTGRES_USER: guaike_user
      POSTGRES_PASSWORD: 20Guaike26.
      POSTGRES_DB: guaike_db
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
